on: 
  - push
  - pull_request
jobs:
  compile-sketch:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - uses: arduino/compile-sketches@v1
        with: 
          sketch-paths: |
            - ./
          libraries: |
            # No 
            - name: Adafruit MPU6050
            - name: Adafruit Unified Sensor
            - name: PCA9685_16-Channel_PWM_Driver_Module_Library

